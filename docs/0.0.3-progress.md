# Panopticon Null v0.0.3 — Build Progress (TEMP)

This file is a temporary working document. It is folded into
`docs/validation-0.0.3.md` and deleted before the final release commit.

## Phase 1: Source survey + authority/coverage model

- [x] Verify starting state (branch `feat/panopticon-null-0.0.3`, HEAD `11011c1`, clean).
- [x] Research live official sources; write `docs/0.0.3-source-survey.md`.
- [x] Source-authority model (enum) + persistent coverage ledger (tables + helpers).

## Phase 2: Schema + ingestion adapters

- [x] v0.0.3 DB migration (SCHEMA_VERSION=2), upgrade + atomicity tests.
- [x] Procurement domain types (matter, event, identifier, money, organization).
- [x] `pnull-procurement` crate: awards parser, solicitation mirror parser, openbook negative finding, operator import. Compiles; 42 unit tests pass; clippy clean.
- [ ] CLI wiring for `procurement`, `coverage`, `case`, `cora` commands (offline default).

## Phase 3: Reconciliation + coverage + change detection

- [x] Reconciliation rules + review queue.
- [x] Immutable snapshots + revision/supersession + record-level diff + 304 handling.
- [ ] Coverage show/diff commands (CLI).

## Phase 4: Case file + CORA

- [x] Case-file generation (JSON + Markdown).
- [x] Gap-driven CORA draft generator.
- [ ] CLI `case build` + `cora draft`.

## Phase 5: Fixtures, hostile tests, docs, validation

- [ ] Real case study (R26-023AB) + benign control fixtures.
- [ ] Hostile tests, property tests, determinism, offline demo.
- [ ] Documentation (README, architecture, methodology, threat-model, roadmap, procurement-methodology, operator/CLI/schema docs, fixture provenance, validation-0.0.3.md).
- [ ] Version bump 0.0.3 + final release commit.
